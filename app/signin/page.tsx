import SigninClient from "./SigninClient";

export const metadata = {
  title: "Sign In",
  description: "Sign in to your Akoustic Arts account",
};

export default function SigninPage() {
  return <SigninClient />;
}
